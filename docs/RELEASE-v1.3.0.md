# v1.3.0 — Component Expansion

## New endpoints

- `/api/pulse`
- `/api/radar`
- `/api/constellation`
- `/api/timeline`
- `/api/repos`
- `/api/year`
- `/api/compare`
- `/api/ticker`

## Data layer

Profile responses now include the user’s recently updated owned repositories for
the Repository Stack component.

## Landing page

The landing page is now entirely product-focused:

- live API output examples,
- categorized endpoint catalog,
- real-data builder,
- request pipeline,
- query parameter reference,
- real-data contract,
- compact component examples,
- Vercel deployment steps.

## Motion

Added floating constellation nodes, radar polygon reveal and live ticker motion.
